git --version
git init
git config --gloabl user.name "neki_user_name"	//global je opcionalno, moze se i za samo jedan folder tj repozitorij
git config user.email "neki_email"	//gloabl opcionalno

mkdir - make a new directory
cd - change current working directory
ls - list the files in the directory

git status
git status --short	//bolje ovako
	?? - Untracked files
	A - Files added to stage
	M - Modified files
	D - Deleted files

u VSC-u znacenje oznaka kraj imena:
A - Added (This is a new file that has been added to the repository)
M - Modified (An existing file has been changed)
D - Deleted (a file has been deleted)
U - Untracked (The file is new or has been changed but has not been added to the repository yet)
C - Conflict (There is a conflict in the file)
R - Renamed (The file has been renamed)
S - Submodule (In repository exists another subrepository)

git add file_name

git add -all	//dodavanje svih file-a u repository

git commit -m "Neka poruka"	

git log	//history of commits for a repository

git help --all
git command -help

Note: If you find yourself stuck in the list view, 
SHIFT + G to jump the end of the list, then q to exit the view.

git branch branch_name
git branch	//shows us every branch we have
			//* beside name shows us that we are currently on that branch

git checkout hello-world-images


/*------------------------------------------------------------------------------------*/


git remote add origin https://github.com/w3schools-test/hello-world.git


for staying up to date you must fetch last version of project
     fetch, merge, pull

Git fetch
     














